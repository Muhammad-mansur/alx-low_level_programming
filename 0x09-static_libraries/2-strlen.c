#include "main.h"
#include <string.h>

/**
  * _strlen - A c function that returns the length of a string
  *
  * @s: character variable
  *
  * Return: 0 (success)
  */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
