#include "main.h"

/**
  * _strcat - concatenation pointer
  *
  * @dest: first pointer
  * @src: second pointer
  *
  * Return: character
  */

char *_strcat(char *dest, char *src)
{
	/* Concatenating s1 and s2 */
	_strcat(dest, src);
	return (dest);

}
