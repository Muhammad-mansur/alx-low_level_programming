#include "main.h"
#include <ctype.h>

/**
  * cap_string - function name
  *
  * @str: string pointer
  *
  * Return: character
  */

char *cap_string(char *str)
{
	char *s = str;
	char *s = toupper(*str);

	return (s);
}

