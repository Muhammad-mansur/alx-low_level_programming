#include "dog.h"

/**
  */

void print_dog(struct dog *d)
{
	struct dog
	{
		char name;
		int age;
		char owner;
	};

	if (name == NULL || age == NULL || owner == NULL)
	{
		printf("nil");
	}

	if (d == NULL)
		return;
}
